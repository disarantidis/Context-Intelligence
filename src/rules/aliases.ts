/**
 * Layer 2 rules — Aliases.
 *
 * Alias chain validity (no broken aliases, direction: Component → Scheme →
 * .mode → foundation → .core). Re-exports the foundation-rules module
 * which encodes this logic.
 */
export * from '../foundation-rules';
