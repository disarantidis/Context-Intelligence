/**
 * Layer 2 rules — Naming.
 *
 * Token path / component naming rules. Re-exports the existing
 * ds-naming-validator implementation so consumers can import from
 * the canonical src/rules/ path without breaking existing callers.
 */
export * from '../ds-naming-validator';
